from aiogram import Router, F, types
from aiogram.types import FSInputFile

router = Router()

@router.message(F.text == "🧘 Ресурсы")
async def resources_menu(message: types.Message):
    # 1. Отправляем Аудио
    try:
        audio = FSInputFile("data/safe_place.mp3")
        await message.answer_audio(audio, caption="🎧 <b>Практика: Безопасное место</b>\nСлушай, когда нужно быстро успокоиться.", parse_mode="HTML")
    except:
        pass # Если файла нет, просто пропускаем

    # 2. Текст с контактами
    await message.answer(
        "🔋 <b>Полезные ресурсы</b>\n\n"
        "<b>📞 Куда звонить (бесплатно, анонимно):</b>\n\n"
        "🇧🇾 <b>Беларусь:</b>\n"
        "• Взрослым: <code>8-017-352-44-44</code> (Минск)\n"
        "• Подросткам: <code>8-801-100-16-11</code>\n\n"
        "🇷🇺 <b>Россия:</b>\n"
        "• Экстренная помощь МЧС: <code>+7-495-989-50-50</code>\n\n"
        "👇 <i>Помни: обращаться за помощью — это признак силы, а не слабости.</i>",
        parse_mode="HTML"
    )
